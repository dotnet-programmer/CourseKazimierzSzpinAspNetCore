@using GymManager.Application.Employees.Queries.GetEmployeePage

@inject IConfiguration configuration

@model EmployeePageDto

@{
	ViewData["Title"] = "Strona główna";
    Layout = $"/Views/{configuration.GetSection("TemplateKey").Value}/Shared/_LayoutMain.cshtml";
}

<vc:main-carousel priority="2" />

<div class="container">
	<div class="row mt-3 mb-5">

		@* tagi HTML nie będą wyświetlane, tylko staną się częścią strony 
			używając Html.Raw trzeba być pewnym, że kod HTML jest bezpieczny bo zostanie wykonany na stronie *@
		@Html.Raw(Model.Content)

	</div>
</div>