namespace GymManager.UI;

// dummy class to group shared resources
public class CommonResources
{
	// ta klasa jest potrzebna w przypadku gdy pliki z tłumaczeniami są ogólne dla całej aplikacji w plikach CommonResources.pl.resx i CommonResources.en.resx,
	// pozostaje pusta, żeby móc odnieść się do tych wspólnych tłumaczeń
}